from django.urls import path

# from accounts.api_endpoints.Cart import ()

urlpatterns = [
    # path('create/', BrandCreateAPIView.as_view(), name='cart-create'),
    # path('delete/<int:id>/', BrandDeleteAPIView.as_view(), name='cart-delete'),
    # path("read/", CartItemListAPIView.as_view(), name="cart-list"),
    # path('read/<int:id>/', BrandRetrieveAPIView.as_view(), name='cart-retrieve'),
    # path('update/<int:id>/', BrandUpdateAPIView.as_view(), name='cart-update'),

]
